1. For running the project or installing dependencies, do not execute anything. Only provide the commands, and I will paste them into the terminal myself.
2. Avoid using deprecated libraries, insecure practices, or experimental features unless explicitly requested.
3. follow component based aproach in react and avoide recursions.